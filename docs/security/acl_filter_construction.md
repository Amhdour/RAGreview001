> This is the client-facing mirror of the PHASE 11 retrieval ACL review file. The canonical evidence copy remains under rag-security-readiness-review/02_evidence/phase_11/.

# ACL Filter Construction

| ACL filter | File/path | Line reference if available | Inputs used | Filter target | Confirmed behavior | Unverified behavior | Evidence label |
| ---------- | --------- | --------------------------- | ----------- | ------------- | ------------------ | ------------------- | -------------- |
| User ACL tokens | `backend/onyx/access/access.py` | `114-127` | User email and anonymous/public token. | Retrieval ACL list. | Returns `prefix_user_email(user.email)` plus `PUBLIC_DOC_PAT` for ordinary users. | Does not prove every backend consumes the list correctly. | CONFIRMED-FINDING |
| Final index filters | `backend/onyx/context/search/pipeline.py` | `126-147` | User ACLs, source filters, document sets, time cutoff, tags, tenant ID, project/persona/attached-doc/hierarchy IDs. | `IndexFilters`. | Centralizes retrieval filter construction before backend dispatch. | Runtime application of the filters remains unverified. | CONFIRMED-FINDING |
| Vespa weightedSet ACL filter | `backend/onyx/document_index/vespa/shared_utils/vespa_request_builders.py` | `176-252` | ACL list, source types, tags, document sets, persona/project IDs, time cutoff, tenant ID. | Vespa YQL filter string. | Builds `weightedSet(access_control_list, ...)` and combines with other filters. | Does not prove query execution results were runtime-validated. | CONFIRMED-FINDING |
| OpenSearch ACL and scope filters | `backend/onyx/document_index/opensearch/search.py` | `1197-1296`, `454-582` | ACL list, sources, tags, document sets, persona/project IDs, time cutoff, tenant ID. | OpenSearch bool filter clauses. | Applies ACL and scope filters to keyword, semantic, and hybrid queries. | Does not prove all retrieval paths use the same builder. | CONFIRMED-FINDING |
| Document access predicate | `backend/onyx/db/document_access.py` | `31-88` | User email, external group IDs, connector status. | Document access query. | Allows only public connectors, public docs, email matches, or group overlap, while excluding deleting connector pairs. | Runtime search usage of this helper is not proven end-to-end. | CONFIRMED-FINDING |
| Document-set visibility filter | `backend/onyx/db/document_set.py` | `40-87`, `164-179` | User role, group membership, public flag, ownership. | Document-set access. | Restricts document-set visibility by user/group/public ownership. | Does not prove the retrieval stack always calls it. | OBSERVATION |
| EE group-aware ACL widening | `backend/ee/onyx/access/access.py` | `43-117` | Group names, external group IDs, public external group IDs, document source, cc_pair state. | EE document ACL construction. | Adds group-aware and external-public semantics to base document ACLs. | Runtime enforcement is still unverified. | OBSERVATION |
