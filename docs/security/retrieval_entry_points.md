> This is the client-facing mirror of the PHASE 11 retrieval ACL review file. The canonical evidence copy remains under rag-security-readiness-review/02_evidence/phase_11/.

# Retrieval Entry Points

| Entry point | Type | File/path | Line reference if available | What enters retrieval/search | Evidence label | Security relevance | Limitation |
| ----------- | ---- | --------- | --------------------------- | ---------------------------- | -------------- | ------------------ | ---------- |
| Search API | Backend route | `backend/onyx/server/features/search/api.py` | `58-147` | `SearchRequest` query, sources, document sets, tags, persona, provider/model, and time cutoff. | CONFIRMED-FINDING | Main programmatic retrieval entry point; passes ACL-derived filters into `SearchTool`. | Source-only; runtime effect not validated. |
| Search UI backend | Backend route | `backend/ee/onyx/server/query_and_chat/search_backend.py` | `51-140` | Search flow classification, search submission, search history, and search filters. | CONFIRMED-FINDING | UI-facing retrieval path; can route queries into search or chat flows. | Source-only; runtime effect not validated. |
| Chat message endpoint | Backend route | `backend/onyx/server/query_and_chat/chat_backend.py` | `528-620` | Chat message input; retrieval occurs indirectly through the chat/search tool chain. | OBSERVATION | Chat is a retrieval-adjacent entry point because the search tool is invoked from the chat loop. | The code reviewed here does not itself prove ACL enforcement on every chat path. |
| Admin search | Backend route | `backend/onyx/server/query_and_chat/query_backend.py` | `34-79` | Admin query text and filter inputs; hidden documents can be included for privileged users. | OBSERVATION | Privileged retrieval path; should be treated separately from ordinary search. | Privileged path is intentional; runtime authorization not validated here. |
| Search frontend helper | Frontend call path | `web/src/ee/lib/search/svc.ts` | `17-95` | Search classification, search submission, and search history fetch requests. | OBSERVATION | Shows how frontend search input reaches backend search endpoints. | Does not itself validate access control. |
| Search controller | Frontend call path | `web/src/ee/providers/QueryControllerProvider.tsx` | `92-238` | Routes search vs chat and sends search requests with filters. | OBSERVATION | Important for user-facing routing into search vs chat. | Client-side routing is not an ACL boundary. |
| Citation rendering | Frontend display path | `web/src/components/search/results/Citation.tsx` | `37-121` | Search-result citation metadata and click-through handling. | OBSERVATION | Citation display can expose source labels and titles if search results are returned. | Rendering behavior is not an ACL check. |
